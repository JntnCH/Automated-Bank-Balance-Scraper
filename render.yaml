services:
  - type: web
    name: bank-scraper
    runtime: python311
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bank-scraper.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: true
